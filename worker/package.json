{
  "name": "@url-shortener/worker",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply url_shortener --local",
    "migrate:remote": "wrangler d1 migrations apply url_shortener --remote"
  },
  "dependencies": {
    "@url-shortener/shared": "*",
    "hono": "^4.5.0",
    "jose": "^5.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.41",
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0"
  }
}
