# url-shortener

A modern, secure, single-user URL shortener on Cloudflare Workers, with a
cross-browser extension. MIT licensed. This repository contains no secrets.

See `docs/superpowers/specs/` for the design and `docs/superpowers/plans/`
for the implementation plans. Deploy guide: below (added during build).
