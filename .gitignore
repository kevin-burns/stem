node_modules/
dist/
.dev.vars
.wrangler/
*.log
.DS_Store

.claude

# Local-only design docs (specs/plans) — kept with the project, not published
docs/

# Build output (dist/) is already ignored above. The Chrome extension ships as a
# GitHub Release asset (chrome.zip). Firefox isn't distributed — it needs Mozilla
# signing we don't want to redo on every change; build it from source if needed.